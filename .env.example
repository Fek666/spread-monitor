# Password for dashboard access
APP_PASSWORD=webullabuse

# Flask secret key (auto-generated if not set)
SECRET_KEY=

# Session lifetime in days
SESSION_DAYS=30

# Port (Railway sets this automatically)
PORT=8765

# Spread monitor settings (optional overrides)
SPREAD_THRESHOLD=0.7
CHECK_INTERVAL=8
TICKERS=NVDA,TSLA,AAPL,GOOGL,META,MSFT,AMD,AMZN
